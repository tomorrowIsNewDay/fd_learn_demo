;(function($){
  $.fn.move = function(opts){
    var DEFAULTES = {
    
    }
    opts = $.extend(true, DEFAULTES, opts);
    // 初始化事件
    var init = function(){
      
    }
    
    // 初始化
    $(window).load(function(){
        init()
    })
  }

}(jquery));
